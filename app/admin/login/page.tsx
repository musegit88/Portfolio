import Login from "../(authroutes)/_components/login";

const AdminLoginPage = () => {
  return (
    <div className="flex items-center justify-center h-screen w-full">
      <Login />
    </div>
  );
};

export default AdminLoginPage;
