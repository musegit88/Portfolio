const RoutesLayout = ({ children }: { children: React.ReactNode }) => {
  return <main className="min-h-dvh snap-y snap-mandatory scroll-smooth space-y-24">{children}</main>;
};

export default RoutesLayout;
